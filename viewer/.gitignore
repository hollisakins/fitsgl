node_modules/
# The vendor build goes to ../fitsgl-py/src/fitsgl/_viewer (vendored, committed).
# dist/ appears only for dev/preview builds (FITSGL_VIEWER_OUTDIR=dist, see
# docs/dev-preview.md) and is ignored by the root .gitignore dist/ rule.
