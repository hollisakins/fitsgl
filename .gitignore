__pycache__/
*.pyc
.pytest_cache/
.coverage
*.egg-info/
build/
dist/
node_modules/
.DS_Store

# Deploy secrets — never commit (`fitsgl deploy` reads these; see docs/r2-setup.md)
.env
.env.*
*.env

# The vendored SSG viewer bundle is a committed build artifact — keep it tracked
# even if a broader ignore rule is added above later.
!fitsgl-py/src/fitsgl/_viewer/
!fitsgl-py/src/fitsgl/_viewer/**
